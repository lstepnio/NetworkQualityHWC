<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--
        Needed to install APKs downloaded via PackageInstaller while the app
        ships as a normal user-installable. When we pivot to a system app
        signed with the platform key, this permission can be replaced with
        the signature-protected INSTALL_PACKAGES and the system "Install
        update?" dialog is skipped (see plan: ~/.claude/plans/rosy-forging-toast.md).
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".FisionApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/banner"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetworkQualityHWC">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/Theme.NetworkQualityHWC">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Receives PackageInstaller status broadcasts after we commit an
            update session. Sees STATUS_PENDING_USER_ACTION (launch confirm
            dialog), STATUS_SUCCESS, and the various STATUS_FAILURE_*
            variants. Not exported — only PackageInstaller itself targets us
            via the PendingIntent we hand to commit().
        -->
        <receiver
            android:name=".update.UpdateInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hotwire.fisiontv.networkqual.update.INSTALL_RESULT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
